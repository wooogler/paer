{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Rebuilding everything from scratch v2' && pnpm install && echo 'Building shared package' && pnpm build:shared && echo 'Building client with new settings' && pnpm build:client && echo 'Building server' && pnpm build:server && echo 'Build completed' && echo 'Client dist contents:' && ls -la client/dist"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "NODE_ENV=production RAILWAY_ENVIRONMENT=true PORT=$PORT MONGO_URL=$MONGO_URL node server/dist/server/src/index.js",
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
