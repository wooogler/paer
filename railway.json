{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && echo 'Building shared package' && pnpm build:shared && echo 'Building client' && pnpm build:client && echo 'Building server' && pnpm build:server && echo 'Build completed'"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "NODE_ENV=production RAILWAY_ENVIRONMENT=true PORT=$PORT node server/dist/server/src/index.js",
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
